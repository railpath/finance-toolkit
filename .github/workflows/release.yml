name: Release to npm

on:
  push:
    tags:
      - 'v*'  # Triggers on version tags like v1.0.0, v1.2.3, etc.
  workflow_dispatch:  # Allows manual triggering from GitHub UI

jobs:
  release:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch all history for proper versioning
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          registry-url: 'https://registry.npmjs.org'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run tests
        run: npm test
      
      - name: Run linting
        run: npm run lint
      
      - name: Build package
        run: npm run build
      
      - name: Publish to npm
        run: |
          echo "Using NPM_TOKEN for authentication"
          npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_AUTOMATION_TOKEN }}
      
      - name: Create GitHub Release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref_name }}
          release_name: Release ${{ github.ref_name }}
          body: |
            ## 🚀 @railpath/finance-toolkit ${{ github.ref_name }}
            
            ### 📦 Installation
            ```bash
            npm install @railpath/finance-toolkit@${{ github.ref_name }}
            ```
            
            ### 🔗 Links
            - 📦 [npm Package](https://www.npmjs.com/package/@railpath/finance-toolkit)
            - 📖 [Documentation](https://github.com/railpath/finance-toolkit#readme)
            - 🐛 [Report Issues](https://github.com/railpath/finance-toolkit/issues)
            
            ### 📊 Package Statistics
            - ✅ **829 Tests** - All passing
            - 🔷 **TypeScript** - Fully typed with Zod validation
            - 📦 **ES Modules** - Modern JavaScript support
            - 🏗️ **Size**: 39.9 kB (177.0 kB unpacked)
            
            ### 🎯 Features
            - Portfolio performance metrics (TWR, MWR)
            - Risk analytics (VaR, Expected Shortfall, Sharpe Ratio)
            - Volatility calculations (EWMA, Parkinson, Garman-Klass)
            - Portfolio optimization and rebalancing
            - Performance attribution analysis
          draft: false
          prerelease: false
